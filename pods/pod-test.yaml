apiVersion: v1
kind: Pod
metadata:
  name: test
  labels:
    name: test
spec:
  containers:
  - name:  test
    image: ntxvm015.iccluster.epfl.ch/mlo/test
    command:
      - sleep
      - infinity
    resources:
      limits:
        nvidia.com/gpu: 4 # requesting 2 GPUs
