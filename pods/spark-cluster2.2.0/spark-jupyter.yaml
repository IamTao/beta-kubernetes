kind: Pod
apiVersion: v1
metadata:
  name: jupyter
spec:
  containers:
    - name: pyspark
      image: ntxvm015.iccluster.epfl.ch/icit-images/spark-jupyter:latest
      imagePullPolicy: Always
      command: 
      - sleep
      - infinity
      ports:
        - containerPort: 8081
      volumeMounts:
      - mountPath: /icit
        name: icit
      - mountPath: /opt/spark/conf
        name: spark-config
  volumes:
    - name: icit
      persistentVolumeClaim:
        claimName:  ic-it-scratch
    - name: spark-config
      configMap:
        name: spark-cm
