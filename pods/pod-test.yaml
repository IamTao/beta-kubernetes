apiVersion: v1
kind: Pod
metadata:
  name: test
  labels:
    name: test
spec:
  containers:
  - name:  test
    image: ntxvm015.iccluster.epfl.ch/mlo/pytorch-mpi:1.0
    command:
      - sleep
      - infinity
    resources:
      limits:
        nvidia.com/gpu: 2 # requesting 2 GPUs
